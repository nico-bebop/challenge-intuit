version: '3.9'

services:

  api:
    build:
      context: .
      dockerfile: backend/Dockerfile

    ports:
      - "5000:8080"

  frontend:
    build:
      context: ./frontend

    ports:
      - "5173:5173"

    depends_on:
      - api